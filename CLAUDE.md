# 저장소 컨벤션

## 테스트 컨벤션

- 테스트는 기본적으로 단위 테스트(unit test)로 작성한다.
- 실패 원인이 국소적이고 읽기 쉽게 드러나도록, 테스트 파일을 대상 메서드나 동작 단위로 분리한다.

## gRPC 서비스 컨벤션

- 검증 로직은 `validator/GrpcValidator` 에 두고 gRPC 서비스에 주입한다.
- 매핑 로직은 더 강력한 이유가 없다면 `mapper/GrpcMapper` 의 static 헬퍼에 유지한다.

## TDD 워크플로

백엔드 기능 작업은 Red-Green-Refactor 를 따른다.

1. Red: 실패하는 테스트를 먼저 작성하거나 수정하고, 실패를 확인한다.
2. Green: 대상 테스트를 통과시키는 최소한의 프로덕션 코드 변경을 구현한다.
3. Refactor: 테스트 스위트를 green 으로 유지한 채 코드를 정리한다.

## Redis 컨벤션

- 키 구분자는 `:` 를 사용한다. 예: `channel:1:channel_names`.
- 캐시 표현식에서 메서드 파라미터가 필요하면 `#p0`, `#p1` 처럼 인덱스 기반 SpEL 접근을 선호한다.

## 모델 선택 컨벤션

- 복잡한 작업(설계)은 opus 모델로 진행한다. 예: 요구사항 분석, 아키텍처/영향 범위 설계, plan 작성.
- 간단한 작업(구현)은 sonnet 모델로 진행한다. 예: plan에 따른 코드 구현, 테스트 작성, 기계적 수정.

## Git 커밋 컨벤션

- git 커밋 메시지는 한국어로 작성한다.

## 백엔드 배포 하네스

- 백엔드 구현 작업은 저장소 이슈 목록과 선택된 GitHub 이슈에서 시작한다.
- 요청 분석, 설계, 구현, PR 작업을 시작하기 전에 `_workspace/00_input/issue-selection.md` 를 보존한다.
- 이슈 인테이크, 계획 수립, TDD, 리뷰, PR 발행에는 `.agents/skills/` 와 `docs/harness/backend-feature-delivery/` 아래의 백엔드 기능 배포 하네스를 사용한다.
